# توثيق عمليات التطبيع في مشروع Quran Roots App Pro

يحتوي المشروع على عدة عمليات لتطبيع النصوص (Normalization) لضمان دقة البحث والمطابقة، سواء كانت مدخلات من المستخدم أو مخرجات من قاعدة البيانات.

## 1. تطبيع مدخلات المستخدم (User Input Normalization)

تتم معالجة ما يدخله المستخدم في خانات البحث لضمان البحث الفعال حتى مع وجود اختلافات في الكتابة أو التشكيل.

### أ. إزالة التشكيل (Tashkeel Removal)
يتم حذف جميع حركات الإعراب والتشكيل والعلامات القرآنية من نص البحث:
- **الحركات الأساسية:** الفتحة، الضمة، الكسرة، السكون، الشدة.
- **التنوين:** تنوين الفتح، الضم، الكسر.
- **العلامات القرآنية:** الألف الخنجرية (`\u0670`) وعلامات الوقف والتجويد.
- **المكان:** 
    - واجهة المستخدم: `client/src/lib/searchService.ts` دالة `removeTashkeel`.
    - الخادم: `backend/src/services/rootService.js` دالة `suggestRoots` و `searchByRoot`.

### ب. توحيد الأحرف (Character Unification)
يتم توحيد أشكال كتابة الألف والهمزات لتجنب أخطاء الإملاء:
- **توحيد الهمزات:** تحويل (`ٱ أ إ آ ؤ ئ ء`) إلى ألف عادية (`ا`).
- **إزالة التطويل (Kashida):** حذف حرف المد (`ـ`) المستخدم لتطويل الكلمات.
- **المكان:** 
    - واجهة المستخدم: `client/src/lib/searchService.ts` دالة `normalizeForSearch`.
    - واجهة المستخدم: `client/src/components/ui/AyahCard.tsx` دالة `normalizeForMatch`.

---

## 2. تطبيع مخرجات قاعدة البيانات (Database Output Normalization)

يتم تخزين نص القرآن بالرسم العثماني (بالتشكيل)، ولكن عند إجراء عمليات المقارنة أو البحث، يتم التطبيع برمجياً.

### أ. المطابقة الذكية للكلمات (Smart Word-to-Root Mapping)
عندما يدخل المستخدم كلمة مثل "المؤمنين"، يقوم النظام بالمطابقة كالتالي:
1. تطبيع الكلمة المدخلة (إزالة "ال" التعريف أحياناً أو مجرد إزالة التشكيل).
2. البحث في جدول الكلمات (`token`) عن كلمة تطابق المدخل المطبّع.
3. استخراج "الجذر" (Root) المرتبط بتلك الكلمة من قاعدة البيانات.
4. عرض النتائج لهذا الجذر.
- **المكان:** `backend/src/services/rootService.js` في مرحلة `SMART SEARCH PHASE 2`.

### ب. المقارنة داخل الواجهة (UI-side Matching)
عند عرض الآية وتلوين الكلمات التي تنتمي للجذر المختار:
- يتم أخذ كل كلمة في الآية وتطبيعها باستخدام دالة `normalizeForMatch`.
- مقارنة الكلمة المطبّعة مع الجذر المطبّع.
- **المكان:** `client/src/components/ui/AyahCard.tsx` داخل مكون `HighlightableText`.

---

## 3. ملخص التقنيات المستخدمة

| العملية | التقنية | النطاق (Regex) |
| :--- | :--- | :--- |
| **إزالة التشكيل** | JavaScript / SQL Replace | `[\u064B-\u065F\u0670]` |
| **توحيد الهمزات** | JavaScript Replace | `[ٱأإآؤئء] -> ا` |
| **إزالة الرموز** | JavaScript Replace | `[^\w\s\u0600-\u06FF]` |
| **تطبيع قاعدة البيانات** | SQL Multi-Replace | 9 مستويات من `REPLACE` في الاستعلام |

---

## 4. الفهرس الثابت (Static Word Index)
لتحسين الأداء في الواجهة الأمامية، يتم تحميل فهرس ثابت (`word-index`) يحتوي على خريطة (Map) تربط الكلمات بجذورها، ويتم تطبيع مفاتيح هذا الفهرس مسبقاً لضمان سرعة الاقتراحات (Suggestions).
- **المكان:** `client/src/contexts/QuranContext.tsx` دالة `suggestRoots`.
